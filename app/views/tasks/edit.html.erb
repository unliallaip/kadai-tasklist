<h1>id: <%= @task.id %> のタスク編集ページ</h1>

<%= form_with(model: @task, local: true) do |f| %>
  <%= f.label :content, 'メッセージ' %>
  <%= f.text_field :content %>

  <%= f.submit '追加' %>
<% end %>

<%= link_to 'タスク一覧に戻る', messages_path %>
<%= link_to 'このタスク内容を編集する', edit_task_path(@task) %>